/**
 * Stub for the `server-only` package.
 *
 * That package intentionally throws when imported outside a React Server
 * Component. Vitest aliases it here so service modules — which mark themselves
 * server-only in the application — can still be unit- and integration-tested.
 */
export {};
